import type { TreeNodeData } from "./types";

export const createExpressionStatementCard = (): TreeNodeData => ({
    title: "Expression Statement",
    rows: [],
    config: {
        surface: "amberSoft",
        accent: "amber",
    },
});
